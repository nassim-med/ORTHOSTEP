import "../../app/globals.css";

export const metadata = {
  title: "ORTHOSTEP Admin",
  description: "Panneau d'administration ORTHOSTEP pour gérer produits, commandes et livraisons.",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
